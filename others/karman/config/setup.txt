# Simulation Setup

# Function for initial conditions
# Choose one of: initial_karman
initial_conditions = inital

# Function for boundary conditions
# Choose one of: periodic_bc, neumann_bc, karman_bc
boundary_conditions = no_bc


# Time parameters
t_start = 0      # Start time
t_end = 200       # End time
dt = 0.01           # Time step (if zero, the CFL condition is used)
dt_io = 10     # Time step for input/output

# Name for the simulation
folder_path = results/            # Folder to save simulation results

# Spatial parameters
dx = 0.1        # Grid step
dy = 0.1
xmin = 0 
xmax = 30   
ymin = 0
ymax = 10

# Fluid properties
gamma = 1.4      # Ratio of specific heats (for compressible flow)

# CFL condition
cfl = 0.4        # CFL number for stability (only used if dt = 0)